name: Airprobe
description: 'ECE497 project in UIC'
publish_to: 'none'

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  charts_flutter: ^0.6.0
  # flutter_blue: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_blue:
    path: ../

flutter:
  uses-material-design: true
  # fonts:
  #     - family: Digital
  #       fonts:
  #         - asset: fonts/digital-7/digital-7 (mono).ttf
  #           weight: 500